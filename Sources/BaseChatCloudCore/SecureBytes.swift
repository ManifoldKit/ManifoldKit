// SecureBytes lives in BaseChatInference/Security/SecureBytes.swift.
// BaseChatCloudCore accesses it via `import BaseChatInference`. This file
// is intentionally a placeholder that documents the canonical location so
// `git grep SecureBytes Sources/BaseChatCloudCore` returns a hit pointing at
// the right module.
