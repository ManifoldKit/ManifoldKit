// SecureBytes lives in ManifoldSecrets/SecureBytes.swift.
// ManifoldCloudCore accesses it via `import ManifoldInference` (which
// @_exported-imports ManifoldSecrets). This file is intentionally a
// placeholder that documents the canonical location so
// `git grep SecureBytes Sources/ManifoldCloudCore` returns a hit pointing at
// the right module.
