// SecureBytes lives in ManifoldInference/Security/SecureBytes.swift.
// ManifoldCloudCore accesses it via `import ManifoldInference`. This file
// is intentionally a placeholder that documents the canonical location so
// `git grep SecureBytes Sources/ManifoldCloudCore` returns a hit pointing at
// the right module.
