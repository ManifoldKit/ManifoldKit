name: CI

on:
  push:
    branches: [main]
    paths:
      - "Sources/**"
      - "Tests/**"
      - "Package.swift"
      - "Package.resolved"
      - ".github/workflows/ci.yml"
      - "scripts/test.sh"
      - "scripts/example-ui-tests.sh"
      - "scripts/ci-normalize-swiftpm-debug-cache.sh"
  pull_request:
    branches: [main]
    paths:
      - "Sources/**"
      - "Tests/**"
      - "Package.swift"
      - "Package.resolved"
      - ".github/workflows/ci.yml"
      - "scripts/test.sh"
      - "scripts/example-ui-tests.sh"
      - "scripts/ci-normalize-swiftpm-debug-cache.sh"

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    runs-on: macos-15  # Apple Silicon (arm64) standard runner to reduce spend

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          # paths-filter needs the parent commit to diff against on `push` events;
          # `pull_request` works with the default shallow clone.
          fetch-depth: 2

      - name: Detect dependency-graph changes
        id: deps-changed
        uses: dorny/paths-filter@v4
        with:
          filters: |
            deps:
              - 'Package.swift'
              - 'Package.resolved'

      - name: Select Xcode
        id: setup-xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          # Pin to a specific version for reproducible builds and to skip the
          # version-discovery API call that `latest-stable` requires.
          xcode-version: "26.3"

      - name: Cache SwiftPM dependencies
        uses: actions/cache@v5
        with:
          # Keep dependency material in the broad Package.resolved cache: .build/artifacts
          # holds the prebuilt llama.cpp xcframework (~100 MB, the real win), and
          # .build/checkouts + .build/repositories save the per-dep clone phase.
          # .build/debug is measured separately below for #953 Lever C so a stale
          # incremental-build cache cannot evict or destabilize this dependency cache.
          path: |
            .build/artifacts
            .build/checkouts
            .build/repositories
          key: ${{ runner.os }}-${{ runner.arch }}-xcode-26.3-spm-${{ hashFiles('Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-xcode-26.3-spm-
            ${{ runner.os }}-${{ runner.arch }}-spm-

      - name: Cache SwiftPM debug build (Lever C spike)
        id: swiftpm-debug-cache
        uses: actions/cache@v5
        with:
          # Lever C (#953) spike: re-test .build/debug with a narrow content key and
          # post-restore mtime normalization instead of folding it into the broad
          # dependency cache above. The key includes package pins, Package.swift,
          # Sources, and Tests so restored object/module files match the checked-out
          # content exactly; no restore-keys are used because touching stale objects
          # newer than changed sources could hide rebuild work. Real evidence still
          # requires PR/main runs: compare this step's hit state plus the test-step
          # durations and the final debug-cache size report before deciding whether
          # to keep or remove the spike.
          path: .build/debug
          key: ${{ runner.os }}-${{ runner.arch }}-xcode-26.3-spm-debug-${{ hashFiles('Package.resolved', 'Package.swift', 'Sources/**/*.swift', 'Tests/**/*.swift') }}

      - name: Normalize SwiftPM debug cache mtimes
        if: steps.swiftpm-debug-cache.outputs.cache-hit == 'true'
        run: scripts/ci-normalize-swiftpm-debug-cache.sh .build/debug

      - name: Verify Swift toolchain matches Package.swift tools-version
        # The runner's Xcode (pinned above) ships a specific Swift version. If
        # `swift-tools-version` in Package.swift exceeds that, `swift package
        # resolve` and every subsequent build will fail with a confusing error.
        # Catch the mismatch here with a clear message naming both versions.
        run: |
          set -euo pipefail
          tools_line=$(head -n 1 Package.swift)
          tools_version=$(printf '%s\n' "$tools_line" | sed -E 's|^//[[:space:]]*swift-tools-version:[[:space:]]*([0-9]+\.[0-9]+).*|\1|')
          if ! [[ "$tools_version" =~ ^[0-9]+\.[0-9]+$ ]]; then
            echo "Could not parse swift-tools-version from first line of Package.swift: $tools_line"
            exit 1
          fi
          swift_version_full=$(xcrun swift -version | head -n 1)
          swift_version=$(printf '%s\n' "$swift_version_full" | sed -E 's|.*Swift version ([0-9]+\.[0-9]+).*|\1|')
          if ! [[ "$swift_version" =~ ^[0-9]+\.[0-9]+$ ]]; then
            echo "Could not parse Swift major.minor from: $swift_version_full"
            exit 1
          fi
          tools_major=${tools_version%.*}; tools_minor=${tools_version#*.}
          swift_major=${swift_version%.*}; swift_minor=${swift_version#*.}
          if (( tools_major > swift_major )) || { (( tools_major == swift_major )) && (( tools_minor > swift_minor )); }; then
            echo "::error::Package.swift requires swift-tools-version $tools_version but runner has Swift $swift_version. Lower swift-tools-version or upgrade the pinned Xcode."
            exit 1
          fi
          echo "swift-tools-version=$tools_version OK against runner Swift $swift_version"

      - name: Lint — BaseChatUI does not import BaseChatUIModelManagement
        # The two modules form a one-way edge: BaseChatUIModelManagement depends
        # on BaseChatUI, never the other way around. A back-edge would close the
        # cycle the v2.0 peel was designed to break. The cycle is currently
        # dissolved by closure-injecting `APIConfigurationView` into ChatView and
        # GenerationSettingsView; this lint guards against accidental regression.
        run: |
          set -euo pipefail
          if grep -RIn --include='*.swift' '^[[:space:]]*import[[:space:]]\+BaseChatUIModelManagement' Sources/BaseChatUI/; then
            echo "::error::BaseChatUI must not import BaseChatUIModelManagement (would close the dep cycle)."
            exit 1
          fi
          echo "BaseChatUI -> BaseChatUIModelManagement edge is clean."

      - name: Lint — BaseChatRuntime must not import SwiftData or SwiftUI
        # BaseChatRuntime is a pure inference-orchestration module: no persistence
        # framework, no UI framework, no Observation. This keeps it usable from
        # any context that doesn't need a full app stack.
        run: |
          set -euo pipefail
          if grep -rn --include='*.swift' 'import SwiftData\|import SwiftUI\|import Observation' Sources/BaseChatRuntime/; then
            echo "::error::BaseChatRuntime must not import SwiftData, SwiftUI, or Observation."
            exit 1
          fi
          echo "BaseChatRuntime framework-isolation posture OK."

      - name: Lint — BaseChatRuntime must not use URLSession or Keychain
        # BaseChatRuntime must not perform networking or access the system keychain
        # directly — those concerns belong in BaseChatInference or BaseChatBackends.
        run: |
          set -euo pipefail
          if grep -rn --include='*.swift' 'URLSession\|SecItemCopyMatching\|kSecClass' Sources/BaseChatRuntime/; then
            echo "::error::BaseChatRuntime must not use URLSession or Keychain."
            exit 1
          fi
          echo "BaseChatRuntime networking/keychain posture OK."

      - name: Lint — BaseChatUI must not import BaseChatPersistenceSwiftData
        # BaseChatUI must not reach the SwiftData adapter layer directly.
        # All persistence access goes through the port protocols in BaseChatRuntime.
        #
        run: |
          set -euo pipefail
          if grep -rn --include='*.swift' 'import BaseChatPersistenceSwiftData' Sources/BaseChatUI/; then
            echo "::error::BaseChatUI must not import BaseChatPersistenceSwiftData directly."
            exit 1
          fi
          echo "BaseChatUI -> BaseChatPersistenceSwiftData edge is clean."

      - name: Lint — Macro plugin builds inside the SwiftPM sandbox
        # SwiftPM compiles macro plugins (`BaseChatMacrosPlugin`) inside a
        # sandbox-exec jail by default on macOS. The jail blocks network
        # access and most filesystem writes, which is the only thing
        # standing between a compromised swift-syntax dependency and
        # exfiltrating local secrets at compile time (see #726).
        #
        # `Package.swift` can opt out of the jail via `unsafeFlags`. This
        # lint refuses to merge any change that introduces such an opt-out
        # in the macro plugin target or globally.
        run: |
          set -euo pipefail
          if grep -nE 'unsafeFlags|--disable-sandbox|SWIFTPM_DISABLE_SANDBOX' Package.swift; then
            echo "::error::Package.swift must not disable the SwiftPM macro/build sandbox."
            echo "::error::Macro plugins (BaseChatMacrosPlugin) must run sandboxed — see issue #726."
            exit 1
          fi
          # Defensive: guard against anyone wiring --disable-sandbox into a
          # workflow's swift build/test invocation. Excluded paths are this
          # file's own commentary and the threat-model docs that quote the flag.
          if grep -RIn --include='*.yml' --exclude='ci.yml' -- '--disable-sandbox' .github/workflows/; then
            echo "::error::Workflow scripts must not pass --disable-sandbox to swift build/test."
            exit 1
          fi
          echo "Macro plugin sandbox posture OK."

      - name: Verify Package.resolved is up to date
        # Was a separate `resolve-check` job on its own macos-15 runner; folding
        # it into `test` removes one 10×-billed runner per PR and eliminates the
        # second queue wait that `needs: [resolve-check]` imposed.
        # Gated on the deps-changed filter: Package.resolved can only go stale
        # when Package.swift is edited, so skipping when neither file changed
        # avoids ~47s of wasted resolve work on the typical PR (~15% of total CI).
        if: steps.deps-changed.outputs.deps == 'true'
        run: |
          swift package resolve
          git diff --exit-code Package.resolved || \
            (echo "Package.resolved is stale — run 'swift package resolve' and commit the result." && exit 1)

      # CI smoke profile (GitHub macOS Apple Silicon runners):
      # - Runs BaseChatCoreTests + BaseChatRuntimeTests + BaseChatPersistenceSwiftDataTests
      #   + BaseChatInferenceTests + BaseChatInferenceSwiftTestingTests
      #   + BaseChatMCPTests + BaseChatUITests + BaseChatBackendsTests + BaseChatTestSupportTests.
      # - BaseChatInferenceTests contains XCTest suites only; Swift Testing suites live in
      #   BaseChatInferenceSwiftTestingTests to prevent a libmalloc SIGABRT from mixed harnesses.
      # - --disable-default-traits skips MLX (default-enabled) to avoid metallib
      #   crashes in CI — Metal shaders are only compiled by Xcode.
      # - BaseChatBackendsTests without traits runs only CI-safe cloud/SSE tests.
      # - Server tests run in the sibling `server-tests` job behind a paths
      #   filter so Hummingbird only compiles when server code changes.
      # - Macro tests run in the sibling `macros-tests` job behind a paths
      #   filter so swift-syntax (~647 files) only compiles when macro
      #   code changes.
      # Local-only coverage:
      # - Run `swift test --filter BaseChatBackendsTests --traits MLX,Llama` on
      #   Apple Silicon for MLX/llama.cpp paths.
      # - Run `swift test --filter BaseChatE2ETests` on physical hardware.
      # - MCP E2E stays out of per-PR CI; nightly-slow-tests runs the
      #   RUN_MCP_E2E-gated streamable-HTTP smoke with a narrow filter so
      #   npx/network-backed subprocess tests remain local-only.

      # --skip-update: Package.resolved pins every dependency, so the graph is
      # already up to date (the Verify step re-confirms this when deps changed).
      # Without this flag, every `swift build`/`swift test` invocation re-contacts
      # every git remote (~10-15s).
      #
      # The XCTest batch below compiles the test bundle on first invocation,
      # so a separate `swift build --build-tests` step would be duplicate work.

      # Run the full XCTest batch with --parallel. Every test that read or wrote
      # `UserDefaults.standard` has been migrated to a per-suite
      # `UserDefaults(suiteName:)` instance — see
      # `Tests/BaseChatCoreTests/UserDefaultsStandardAuditTest` (the tripwire
      # that fails CI if a future test reintroduces the bare `.standard`
      # access) and issue #910 for the migration history. Combined with
      # MockURLProtocol's per-test unique hosts, the XCTest batch is now
      # safe under SwiftPM's per-process worker model.
      #
      # Swift Testing must stay isolated — mixing XCTest + Swift Testing in one
      # swift test process triggers a libmalloc double-free SIGABRT (see
      # SwiftTestingAuditTest, #681).
      - name: Test — XCTest suites (Core + Runtime + PersistenceSwiftData + UI + UIModelManagement + MCP + Backends + TestSupport + AppIntents + Inference, parallel)
        id: test-xctest-suites
        timeout-minutes: 12
        env:
          BASECHAT_TEST_OUTPUT_FILE: ${{ github.workspace }}/test-diagnostics/test-xctest-suites.log
        run: scripts/test.sh --filter BaseChatCoreTests --filter BaseChatRuntimeTests --filter BaseChatPersistenceSwiftDataTests --filter BaseChatUITests --filter BaseChatUIModelManagementTests --filter BaseChatMCPTests --filter BaseChatBackendsTests --filter BaseChatTestSupportTests --filter BaseChatAppIntentsTests --filter BaseChatInferenceTests --disable-default-traits --skip-update --parallel

      # `if: always()` so a failing XCTest step still surfaces Swift Testing
      # failures in the same run — otherwise breakage in both harnesses costs
      # two re-push cycles to triage. The job still fails because step 1 failed.
      - name: Test — Inference (Swift Testing, isolated process)
        id: test-inference-swift-testing
        if: always()
        timeout-minutes: 12
        env:
          BASECHAT_TEST_OUTPUT_FILE: ${{ github.workspace }}/test-diagnostics/test-inference-swift-testing.log
        run: scripts/test.sh --filter BaseChatInferenceSwiftTestingTests --disable-default-traits --skip-update

      - name: Report SwiftPM debug cache state
        if: always()
        run: |
          set -euo pipefail
          echo "debug-cache-hit=${{ steps.swiftpm-debug-cache.outputs.cache-hit }}"
          if [ -d .build/debug ]; then
            du -sh .build/debug || true
            primary_files=$(find .build/debug -type f \( -name '*.o' -o -name '*.swiftmodule' -o -name '*.swiftdeps' \) -print | wc -l | tr -d ' ')
            echo "debug-cache-primary-files=${primary_files}"
          else
            echo ".build/debug not present"
          fi

      - name: Stage test diagnostics
        # Each test step writes to a unique workspace log path via
        # BASECHAT_TEST_OUTPUT_FILE so `if: always()` follow-up steps cannot
        # overwrite a previous failing step's diagnostics.
        if: failure()
        env:
          XCTEST_SUITES_FAILED: ${{ steps.test-xctest-suites.outcome == 'failure' }}
          INFERENCE_SWIFT_TESTING_FAILED: ${{ steps.test-inference-swift-testing.outcome == 'failure' }}
        run: |
          mkdir -p test-diagnostics
          {
            echo "run-id=${{ github.run_id }}"
            echo "run-attempt=${{ github.run_attempt }}"
            echo "job=test"
            echo "xctest-suites-failed=${XCTEST_SUITES_FAILED}"
            echo "inference-swift-testing-failed=${INFERENCE_SWIFT_TESTING_FAILED}"
          } > test-diagnostics/failed-steps.txt
          # Best-effort: copy any xcresult bundles a future xcodebuild step
          # might have produced under DerivedData.
          find "${HOME}/Library/Developer/Xcode/DerivedData" -type d -name "*.xcresult" -maxdepth 6 2>/dev/null \
            | while read -r r; do cp -R "$r" test-diagnostics/ || true; done

      - name: Upload test diagnostics on failure
        # swift test does not produce .xcresult bundles, so we upload whatever
        # diagnostics the failing step left behind (captured stdout/stderr,
        # plus any xcresult bundles if they exist).
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: xcresult-${{ github.run_attempt }}
          if-no-files-found: ignore
          retention-days: 7
          path: test-diagnostics/

  # Server tests are split out of the per-PR mega-batch because the
  # Hummingbird transitive graph (~18 extra pins, swift-nio + NIOSSL +
  # BoringSSL + AsyncHTTPClient + ContainersPreview + Configuration) added
  # ~250s to the build step when it shipped unconditionally. The Server trait
  # peels Hummingbird out of the default-trait build so only this job pays
  # the compile cost — and only when server code or the dep graph changes.
  server-tests:
    runs-on: macos-15  # match the `test` job runner so cache keys collide

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          # paths-filter needs the parent commit to diff against on `push` events;
          # `pull_request` works with the default shallow clone.
          fetch-depth: 2

      - name: Detect server-relevant changes
        id: server-changed
        uses: dorny/paths-filter@v4
        with:
          filters: |
            server:
              - 'Sources/BaseChatServer*/**'
              - 'Tests/BaseChatServerTests/**'
              - 'Package.swift'
              - 'Package.resolved'
              - '.github/workflows/ci.yml'
              - 'scripts/test.sh'

      - name: Select Xcode
        if: steps.server-changed.outputs.server == 'true'
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.3"

      - name: Cache SwiftPM build
        if: steps.server-changed.outputs.server == 'true'
        uses: actions/cache@v5
        with:
          # Same path/key shape as the `test` job — both jobs share the same
          # SwiftPM dep tarball cache so we don't re-download the graph twice
          # per workflow run.
          path: |
            .build/artifacts
            .build/checkouts
            .build/repositories
          key: ${{ runner.os }}-${{ runner.arch }}-xcode-26.3-spm-${{ hashFiles('Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-xcode-26.3-spm-
            ${{ runner.os }}-${{ runner.arch }}-spm-

      - name: Verify Swift toolchain matches Package.swift tools-version
        if: steps.server-changed.outputs.server == 'true'
        run: |
          set -euo pipefail
          tools_line=$(head -n 1 Package.swift)
          tools_version=$(printf '%s\n' "$tools_line" | sed -E 's|^//[[:space:]]*swift-tools-version:[[:space:]]*([0-9]+\.[0-9]+).*|\1|')
          if ! [[ "$tools_version" =~ ^[0-9]+\.[0-9]+$ ]]; then
            echo "Could not parse swift-tools-version from first line of Package.swift: $tools_line"
            exit 1
          fi
          swift_version_full=$(xcrun swift -version | head -n 1)
          swift_version=$(printf '%s\n' "$swift_version_full" | sed -E 's|.*Swift version ([0-9]+\.[0-9]+).*|\1|')
          if ! [[ "$swift_version" =~ ^[0-9]+\.[0-9]+$ ]]; then
            echo "Could not parse Swift major.minor from: $swift_version_full"
            exit 1
          fi
          tools_major=${tools_version%.*}; tools_minor=${tools_version#*.}
          swift_major=${swift_version%.*}; swift_minor=${swift_version#*.}
          if (( tools_major > swift_major )) || { (( tools_major == swift_major )) && (( tools_minor > swift_minor )); }; then
            echo "::error::Package.swift requires swift-tools-version $tools_version but runner has Swift $swift_version. Lower swift-tools-version or upgrade the pinned Xcode."
            exit 1
          fi
          echo "swift-tools-version=$tools_version OK against runner Swift $swift_version"

      - name: Test — Server suite (Server trait)
        id: test-server
        if: steps.server-changed.outputs.server == 'true'
        timeout-minutes: 12
        env:
          BASECHAT_TEST_OUTPUT_FILE: ${{ github.workspace }}/test-diagnostics/test-server.log
        run: scripts/test.sh --filter BaseChatServerTests --disable-default-traits --traits Server --skip-update

      - name: Stage test diagnostics
        if: failure() && steps.server-changed.outputs.server == 'true'
        run: |
          mkdir -p test-diagnostics
          {
            echo "run-id=${{ github.run_id }}"
            echo "run-attempt=${{ github.run_attempt }}"
            echo "job=server-tests"
            echo "test-server-failed=${{ steps.test-server.outcome == 'failure' }}"
          } > test-diagnostics/failed-steps.txt

      - name: Upload test diagnostics on failure
        if: failure() && steps.server-changed.outputs.server == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: xcresult-server-${{ github.run_attempt }}
          if-no-files-found: ignore
          retention-days: 7
          path: test-diagnostics/

  # Macro plugin tests are split out of the per-PR mega-batch because the
  # `@ToolSchema` macro plugin pulls swift-syntax (~647 source files) into the
  # build graph. Trait-gated behind `Macros` (off by default) so the default
  # `test` job skips the swift-syntax compile cost; this job picks them up
  # only when macro code or the dep graph changes.
  macros-tests:
    runs-on: macos-15  # match the `test` job runner so cache keys collide

    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          # paths-filter needs the parent commit to diff against on `push` events;
          # `pull_request` works with the default shallow clone.
          fetch-depth: 2

      - name: Detect macro-relevant changes
        id: macros-changed
        uses: dorny/paths-filter@v4
        with:
          filters: |
            macros:
              - 'Sources/BaseChatMacrosPlugin/**'
              - 'Sources/BaseChatInference/Macros/**'
              - 'Tests/BaseChatInferenceTests/ToolSchemaMacro*.swift'
              - 'Package.swift'
              - 'Package.resolved'
              - '.github/workflows/ci.yml'
              - 'scripts/test.sh'

      - name: Select Xcode
        if: steps.macros-changed.outputs.macros == 'true'
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.3"

      - name: Cache SwiftPM build
        if: steps.macros-changed.outputs.macros == 'true'
        uses: actions/cache@v5
        with:
          # Same path/key shape as the `test` job — both jobs share the same
          # SwiftPM dep tarball cache so we don't re-download the graph twice
          # per workflow run.
          path: |
            .build/artifacts
            .build/checkouts
            .build/repositories
          key: ${{ runner.os }}-${{ runner.arch }}-xcode-26.3-spm-${{ hashFiles('Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-xcode-26.3-spm-
            ${{ runner.os }}-${{ runner.arch }}-spm-

      - name: Verify Swift toolchain matches Package.swift tools-version
        if: steps.macros-changed.outputs.macros == 'true'
        run: |
          set -euo pipefail
          tools_line=$(head -n 1 Package.swift)
          tools_version=$(printf '%s\n' "$tools_line" | sed -E 's|^//[[:space:]]*swift-tools-version:[[:space:]]*([0-9]+\.[0-9]+).*|\1|')
          if ! [[ "$tools_version" =~ ^[0-9]+\.[0-9]+$ ]]; then
            echo "Could not parse swift-tools-version from first line of Package.swift: $tools_line"
            exit 1
          fi
          swift_version_full=$(xcrun swift -version | head -n 1)
          swift_version=$(printf '%s\n' "$swift_version_full" | sed -E 's|.*Swift version ([0-9]+\.[0-9]+).*|\1|')
          if ! [[ "$swift_version" =~ ^[0-9]+\.[0-9]+$ ]]; then
            echo "Could not parse Swift major.minor from: $swift_version_full"
            exit 1
          fi
          tools_major=${tools_version%.*}; tools_minor=${tools_version#*.}
          swift_major=${swift_version%.*}; swift_minor=${swift_version#*.}
          if (( tools_major > swift_major )) || { (( tools_major == swift_major )) && (( tools_minor > swift_minor )); }; then
            echo "::error::Package.swift requires swift-tools-version $tools_version but runner has Swift $swift_version. Lower swift-tools-version or upgrade the pinned Xcode."
            exit 1
          fi
          echo "swift-tools-version=$tools_version OK against runner Swift $swift_version"

      - name: Test — @ToolSchema macro suite (Macros trait)
        id: test-macros
        if: steps.macros-changed.outputs.macros == 'true'
        timeout-minutes: 12
        env:
          BASECHAT_TEST_OUTPUT_FILE: ${{ github.workspace }}/test-diagnostics/test-macros.log
        run: scripts/test.sh --filter ToolSchemaMacro --disable-default-traits --traits Macros --skip-update

      - name: Stage test diagnostics
        if: failure() && steps.macros-changed.outputs.macros == 'true'
        run: |
          mkdir -p test-diagnostics
          {
            echo "run-id=${{ github.run_id }}"
            echo "run-attempt=${{ github.run_attempt }}"
            echo "job=macros-tests"
            echo "test-macros-failed=${{ steps.test-macros.outcome == 'failure' }}"
          } > test-diagnostics/failed-steps.txt

      - name: Upload test diagnostics on failure
        if: failure() && steps.macros-changed.outputs.macros == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: xcresult-macros-${{ github.run_attempt }}
          if-no-files-found: ignore
          retention-days: 7
          path: test-diagnostics/

  # Cold-start conformance — scaffolds a fresh SwiftPM consumer in a tmpdir,
  # links BCK by local path, and runs one chat turn through a tiny inline fake
  # backend. Catches breaks in the public consumer surface that BCK's own
  # in-tree tests miss (Package.swift link shape, public registration / load /
  # generate APIs, GenerationStream consumption pattern, swift-tools-version
  # floor). See scripts/cold-start-conformance.sh for the full rationale.
  cold-start:
    runs-on: macos-15  # match the `test` job runner so cache keys collide

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Select Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.3"

      - name: Cache SwiftPM build
        uses: actions/cache@v5
        with:
          path: |
            .build/artifacts
            .build/checkouts
            .build/repositories
          key: ${{ runner.os }}-${{ runner.arch }}-xcode-26.3-spm-${{ hashFiles('Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-xcode-26.3-spm-
            ${{ runner.os }}-${{ runner.arch }}-spm-

      - name: Run cold-start conformance
        timeout-minutes: 6
        run: scripts/cold-start-conformance.sh

  # Cold-start conformance (tier 2) — extends the tier-1 pattern to the
  # high-level orchestration surface real chat apps use: BaseChatBootstrap →
  # ChatViewModel → vm.sendMessage(). Catches breaks in BaseChatBootstrap link
  # shape (Inference + Runtime + PersistenceSwiftData + UI products), the
  # configure(runtime:) ergonomics, and the ambient inputText / sendMessage
  # pattern. See scripts/cold-start-tier2-bootstrap.sh for the full rationale.
  cold-start-tier2:
    runs-on: macos-15  # match the `test` job runner so cache keys collide

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Select Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "26.3"

      - name: Cache SwiftPM build
        uses: actions/cache@v5
        with:
          path: |
            .build/artifacts
            .build/checkouts
            .build/repositories
          key: ${{ runner.os }}-${{ runner.arch }}-xcode-26.3-spm-${{ hashFiles('Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-xcode-26.3-spm-
            ${{ runner.os }}-${{ runner.arch }}-spm-

      - name: Run cold-start tier-2 conformance
        timeout-minutes: 6
        run: scripts/cold-start-tier2-bootstrap.sh
